name: CompareVI History Pull Request Diagnostics

on:
  pull_request:
    branches:
      - main
      - develop
      - release/*
      - feature/*
      - hotfix/*
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

permissions:
  contents: read

jobs:
  comparevi-history-pull-request-diagnostics:
    uses: LabVIEW-Community-CI-CD/comparevi-history/.github/workflows/pull-request-diagnostics-auto.yml@v1.3.10
    with:
      consumer_repository: ${{ github.repository }}
      pr_policy_path: .github/comparevi-history-pr-policy.json
      results_dir: tests/results/pr-diagnostics/history
      platform_ref: v1.3.10
